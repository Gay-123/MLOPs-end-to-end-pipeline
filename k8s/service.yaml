apiVersion: v1
kind: Service
metadata:
  name: mlops-service
spec:
  type: NodePort
  selector:
    app: creditcardfraud-app
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30001
    - port: 8000
      targetPort: 8000
      nodePort: 30002
